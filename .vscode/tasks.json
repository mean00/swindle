{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Swindle",
      "type": "shell",
      "command": "make",
      "args": [""],
      "options": {
        "cwd": "${workspaceFolder}/build"
      },
      "group": "build"
    },
{
      "label": "Clean Swindle",
      "type": "shell",
      "command": "make",
      "args": ["clean"],
      "options": {
        "cwd": "${workspaceFolder}/build"
      },
      "group": "build"
    }

  ]
}
