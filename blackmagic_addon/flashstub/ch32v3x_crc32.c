#include "ch32v3x.h"

/*
 */
bool ch32v3x_crc32(uint32_t adr, uint32_t len)
{
    bool ret = false;
qnext:
    if (ret)
    {
        riscv_stub_exit(0);
    }
    else
    {
        riscv_stub_exit(1);
    }
    return ret;
}

//
