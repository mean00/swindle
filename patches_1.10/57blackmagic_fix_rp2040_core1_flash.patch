diff --git a/src/target/cortexm.c b/src/target/cortexm.c
index e166c3c..477b1e4 100644
--- a/src/target/cortexm.c
+++ b/src/target/cortexm.c
@@ -1034,7 +1034,8 @@ bool cortexm_run_stub(target_s *target, uint32_t loadaddr, uint32_t r0, uint32_t
 	regs[3] = r3;
 	regs[15] = loadaddr;
 	regs[CORTEX_REG_XPSR] = CORTEXM_XPSR_THUMB;
-	regs[19] = 0;
+	/* MEANX: Mask all interrupts (PRIMASK=1) to prevent ISRs from crashing the stub */
+	regs[19] = 1;
 
 	cortexm_regs_write(target, regs);
 
