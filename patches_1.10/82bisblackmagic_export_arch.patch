diff --git a/src/include/target.h b/src/include/target.h
index ce0be5d1..e7b4c612 100644
--- a/src/include/target.h
+++ b/src/include/target.h
@@ -38,6 +38,17 @@
 typedef int32_t mode_t;
 #endif /* _MSC_VER */
 
+// MEANX
+#define TOP_ARCH_SHIFT   28
+#define TOPT_GET_ARCH(x) ((x >> TOP_ARCH_SHIFT) & 3)
+#define TOPT_SET_ARCH(value, arch)                                           \
+	{                                                                        \
+		value = (value & ~(3 << TOP_ARCH_SHIFT)) | (arch << TOP_ARCH_SHIFT); \
+	}
+#define TOPT_ARCH_ARM   1
+#define TOPT_ARCH_RISCV 2
+// /MEANX
+
 typedef struct target target_s;
 typedef uint32_t target_addr32_t;
 typedef uint64_t target_addr64_t;
diff --git a/src/target/cortexm.c b/src/target/cortexm.c
index e8ebf4d1..81d3f9ab 100644
--- a/src/target/cortexm.c
+++ b/src/target/cortexm.c
@@ -298,6 +298,8 @@
 	if (!target)
 		return false;
 
+	TOPT_SET_ARCH(target->target_options, TOPT_ARCH_ARM);
+
 	adiv5_ap_ref(ap);
 	if (ap->dp->version >= 2 && ap->dp->target_designer_code != 0) {
 		/* Use TARGETID register to identify target */
diff --git a/src/target/riscv_debug.c b/src/target/riscv_debug.c
index a3d6b3c2..884d3c34 100644
--- a/src/target/riscv_debug.c
+++ b/src/target/riscv_debug.c
@@ -495,6 +495,8 @@
 	if (!target)
 		return false;
 
+	TOPT_SET_ARCH(target->target_options, TOPT_ARCH_RISCV);
+
 	/* Grab a reference to the DMI and DM structures and do preliminary setup of the target structure */
 	riscv_dm_ref(hart->dbg_module);
 	target->driver = "RISC-V";
